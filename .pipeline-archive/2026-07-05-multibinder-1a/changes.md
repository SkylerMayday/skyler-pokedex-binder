# Run 1A — Coder Changes Summary

Implements `.pipeline/specs.md` literally. No scope creep beyond what's specified.

## Files created

### Section 1 — New Room entities
- `app/src/main/java/com/skyler/pokedexbinder/data/local/ConnectingArtGroup.kt` — new entity, table `connecting_art_group` (id, name, rows, cols, position). Matches spec 1.1 verbatim.
- `app/src/main/java/com/skyler/pokedexbinder/data/local/ConnectingArtSlot.kt` — new entity, table `connecting_art_slot`, FK to `ConnectingArtGroup.id` with `ON DELETE CASCADE`, `Index("groupId")`. Matches spec 1.1 verbatim.
- `app/src/main/java/com/skyler/pokedexbinder/data/local/PersonalCollectionCache.kt` — new entity, table `personal_collection_cache`. Matches spec 1.1 verbatim.
- `app/src/main/java/com/skyler/pokedexbinder/data/local/PersonalCollectionEntry.kt` — new entity, table `personal_collection_entry`. Matches spec 1.1 verbatim.

### Section 2 — New DAOs
- `app/src/main/java/com/skyler/pokedexbinder/data/local/ConnectingArtDao.kt` — mirrors `SecondaryBinderDao` conventions (Flow observers, suspend CRUD, `@Transaction createGroupWithSlots`). Matches spec 2.1 verbatim, including the deliberate `insertGroup(): Long` deviation (needed to obtain the generated `groupId` inside the same transaction that creates child slots) and the note that FK cascade delete requires no manual child-row deletion.
- `app/src/main/java/com/skyler/pokedexbinder/data/local/PersonalCollectionDao.kt` — cache + owned-entry CRUD, `@Transaction replaceCacheForPokemon`. Matches spec 2.2 verbatim.

### Section 3 — New Repositories
- `app/src/main/java/com/skyler/pokedexbinder/repository/ConnectingArtRepository.kt` — thin `@Singleton` wrapper over `ConnectingArtDao`, mirrors `BinderRepository` conventions. Matches spec 3.1 verbatim. Card search delegation intentionally NOT implemented here (Run 1B).
- `app/src/main/java/com/skyler/pokedexbinder/repository/PersonalCollectionRepository.kt` — cache/owned-entry persistence + `refreshPokemon(...)` stub with `cardSearchRepository` injected (unused for now, load-bearing for Run 1B per spec). Matches spec 3.2 verbatim.

### Section 5 — Placeholder screens
- `app/src/main/java/com/skyler/pokedexbinder/ui/connectingart/ConnectingArtScreen.kt` — placeholder Scaffold, "Coming soon" body, hamburger `onOpenDrawer`. Matches spec 5.2 verbatim.
- `app/src/main/java/com/skyler/pokedexbinder/ui/personalcollection/PersonalCollectionScreen.kt` — same structure, title "Personal Collection". Matches spec 5.2.

## Files modified

### `app/src/main/java/com/skyler/pokedexbinder/data/local/PokedexDatabase.kt`
- Bumped `@Database` version 6 → 7, added the 4 new entities, added `connectingArtDao()` / `personalCollectionDao()` abstract accessors.
- Added `MIGRATION_6_7` exactly as specified in spec Section 1.2 (verbatim DDL — `INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT` for autogenerated PKs, `INTEGER NOT NULL DEFAULT 0` for the `owned: Boolean` columns, explicit `CREATE INDEX ... index_connecting_art_slot_groupId` matching Room's autogenerated index name, FK with `ON DELETE CASCADE`).
- Did not touch `MIGRATION_4_5` / `MIGRATION_5_6`.

### `app/src/main/java/com/skyler/pokedexbinder/di/DatabaseModule.kt`
- Registered `MIGRATION_6_7` in `addMigrations(...)`.
- Added `@Provides provideConnectingArtDao` and `@Provides providePersonalCollectionDao`.
- Left `.fallbackToDestructiveMigration()` in place per spec's explicit instruction (safety net, not a substitute for a correct migration).
- No `@Provides` added for the two new repositories — they use `@Singleton @Inject constructor`, matching the existing `BinderRepository` pattern (Hilt constructs them automatically).

### `app/src/main/java/com/skyler/pokedexbinder/ui/navigation/AppNavigation.kt`
- Added `Screen.ConnectingArt` ("connecting_art") and `Screen.PersonalCollection` ("personal_collection") routes.
- Wrapped the existing `Scaffold` in a `ModalNavigationDrawer` with `rememberDrawerState`/`rememberCoroutineScope`, and a `ModalDrawerSheet` containing 4 `NavigationDrawerItem`s: Pokédex, Connecting Art, Personal Collection, Card History — each closes the drawer then navigates, matching spec 5.3.
- Removed the `if (settings.showSecondaryBinder) { NavigationBarItem(...) }` block from the bottom `NavigationBar` per spec 5.4/5.6. Kept the Pokédex item and the camera-scanner-conditional Scan item unchanged; `isOnSecondary` computation in the Scan `onClick` untouched (still references `Screen.SecondaryBinder.route`, which remains a live route).
- Added `composable(Screen.ConnectingArt.route) { ConnectingArtScreen(onOpenDrawer = { openDrawer() }) }` and the equivalent for `PersonalCollection`, per spec 5.5.
- Added `onOpenDrawer` param to `MainBinderScreen(...)` call site, wired to `openDrawer()` — per spec 5.5's instruction to make the drawer usable from the default start destination.
- Removed the now-unused `Icons.Default.Menu` import at the top (moved into MainBinderScreen instead) and added the new imports listed in spec 5.3 (`ModalNavigationDrawer`, `ModalDrawerSheet`, `NavigationDrawerItem`, `NavigationDrawerItemDefaults`, `rememberDrawerState`, `DrawerValue`, `rememberCoroutineScope`, `Spacer`/`height`, `dp`, `kotlinx.coroutines.launch`).
- Did not touch `SecondaryBinderScreen`, `SlotDetailScreen`, `QuickScanScreen`, `ScannerScreen`, `SettingsScreen`, or `ManualSearchScreen` composable wiring beyond what's shown above — out of scope for 1A per spec.

### `app/src/main/java/com/skyler/pokedexbinder/ui/mainbinder/MainBinderScreen.kt`
- Added `onOpenDrawer: () -> Unit = {}` parameter.
- Added a `navigationIcon` (hamburger, `Icons.Default.Menu`) to the existing `TopAppBar`, wired to `onOpenDrawer`. This is the "minimal MainBinder hamburger" the spec asks for in 5.5 — no other pre-existing screens were touched (flagged below as an open item per spec's own instruction).

### `app/src/main/java/com/skyler/pokedexbinder/repository/SettingsRepository.kt`
- Removed `showSecondaryBinder` field from `AppSettings`, `Keys.SHOW_SECONDARY_BINDER`, the corresponding line in the `settings` Flow map, and `setShowSecondaryBinder(...)`.

### `app/src/main/java/com/skyler/pokedexbinder/ui/settings/SettingsViewModel.kt`
- Removed `setShowSecondaryBinder(...)`.

### `app/src/main/java/com/skyler/pokedexbinder/ui/settings/SettingsScreen.kt`
- Removed the entire "Navigation" section (`SectionHeader("Navigation")`, its `HorizontalDivider`s, and the Secondary Binder `SettingToggleItem`), since it contained only that one toggle. Confirmed via grep that no other reference to `showSecondaryBinder`/`setShowSecondaryBinder`/`SHOW_SECONDARY_BINDER` remains anywhere under `app/src/main`.
- No DataStore migration/cleanup added for the orphan `"show_secondary_binder"` key — explicitly out of scope per spec 5.6.

### `app/src/main/java/com/skyler/pokedexbinder/repository/CardSearchRepository.kt`
- Modified `safeSearch(query: String)` to append the module-level `POCKET_EXCLUSION` constant (`"-set.series:Pocket"`) to every pokemontcg.io query before calling `api.searchCards(...)`. Blank-query edge case (line 51's `nationalPokedexNumbers` fallback never triggers this, but `safeSearch` is written generically) handled: if `query.isBlank()`, use just the trimmed exclusion string instead of a leading-space-prefixed concatenation.
- Added `companion object { private const val POCKET_EXCLUSION = "-set.series:Pocket" }`.
- Did NOT touch `tcgdexSearchByName` / `TcgdexApi.searchCards` — confirmed by re-reading the file that all pokemontcg.io call sites (the ~15 listed in spec Section 6's table) route through `safeSearch`, so this single edit covers all of them. TCGdex's `@Query("name")` param takes a bare name; appending the DSL filter there would corrupt the search, per spec's explicit scoping decision.

## Deviations from spec

None. Every file matches the spec's provided code verbatim, with only cosmetic reformatting (indentation) in `AppNavigation.kt` to keep the file readable after wrapping the `Scaffold` in `ModalNavigationDrawer`.

## Open item carried forward (per spec's own flag, not a deviation)

Spec Section 5.5 explicitly flags: fully honoring "hamburger icon in each screen's top app bar" would require touching `SecondaryBinderScreen`, `SlotDetailScreen`, `SettingsScreen`, etc. Run 1A's scope (per the spec's own default assumption) is: new screens + MainBinder only. The remaining screens are deferred to Run 1B — this is documented in the spec itself, not something discovered during implementation.

## Build result

- `./gradlew.bat assembleDebug --console=plain` → **BUILD SUCCESSFUL**, zero compile errors. (Some pre-existing warnings unrelated to this change: `ImageProxyExt.kt` shadowed extension, deprecated `LocalLifecycleOwner`, missing `@OptIn` in `SlotDetailViewModel.kt` — none touched by this Run.)
- `./gradlew.bat testDebugUnitTest --console=plain` → compiles cleanly (no test-source-set compile errors caused by this change, so the "move broken test files aside" workaround was not needed). Test run: **57 tests completed, 8 failed** — all 8 failures are in the pre-existing broken set called out in the task brief:
  - `AssignCardUseCaseTest > assign to occupied slot moves old card to secondary binder`
  - `GeminiCardScannerTest` (4 tests: 429/500 exceptions, null-fields mapping, success mapping)
  - `SmartThresholdUseCaseTest > multiple results with name match but no number is low confidence`
  - `BinderRepositoryTest > assignCard upserts entry with card info`
  - `CardSearchRepositoryTest > searchByNameAndNumber builds correct query and maps results`
  - Verified `CardSearchRepositoryTest`'s failure is pre-existing and unrelated to the Pocket filter: the test stubs `coEvery { api.searchCards("name:\"Venusaur\" number:\"1\"") }`, but the repository's real `nameQuery()` builds `name:*Venusaur*` (wildcard form), not `name:"Venusaur"` — a stale-mock mismatch that predates this Run's change (the Pocket suffix would only ever be an *additional* mismatch on top of an already-wrong stub).
  - No files were temporarily moved aside; not fixing any of the 8 per the task brief.

## Verification of migration correctness (for Reviewer/Tester)

- `owned` columns: `INTEGER NOT NULL DEFAULT 0` — matches Room's mapping for non-null `Boolean` with Kotlin default.
- Autogenerate PK columns: `INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT` used verbatim as given in the spec (spec's own note says either form is schema-compatible; used the exact SQL provided rather than the alternate rowid-alias form, since the spec supplied one canonical version to implement).
- Index name `index_connecting_art_slot_groupId` matches Room's autogenerated name for `Index("groupId")` on `connecting_art_slot`.
- FK `ON DELETE CASCADE` relies on Room's default `PRAGMA foreign_keys = ON` — no manual pragma added, no manual child-row deletion added (would double-delete / mask FK bugs per spec's warning).
- Did not run an instrumented `MigrationTestHelper` (no Android device/emulator available in this environment) — flagging for Tester per spec's Build/Verification Checklist item 2/3, which explicitly assigns real-device migration verification to the Tester stage.
